# hubandcircles-manager (repo A)

Python management tool: package `manager/`, Streamlit UI `manager/ui/`, generated `schema/`.
Follow `.claude/skills/manager-dev/SKILL.md`. Architecture: `../ARKKITEHTUURI.md`
(Finnish prose, English identifiers). Streamlit pages: `ADMIN-UI-SPEC.md`.

All code, comments, identifiers, field names, enum values and commit messages are English (P10).

Done: `uv run ruff check . && uv run ruff format --check . && uv run pytest`
